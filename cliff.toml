[changelog]
header = "# Changelog\n\nVšechny změny jsou zdokumentovány v tomto souboru.\nFormát vychází z [Keep a Changelog](https://keepachangelog.com/).\n"
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group }}
{% for commit in commits %}
- {{ commit.message | upper_first }}\
{% if commit.breaking %} ⚠️ BREAKING CHANGE{% endif %}

{% endfor %}
{% endfor %}\n
"""
trim = true
footer = ""

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false

commit_parsers = [
  { message = "^feat",  group = "✨ Nové funkce" },
  { message = "^fix",   group = "🐛 Opravy" },
  { message = "^perf",  group = "⚡ Výkon" },
  { message = "^refactor", group = "♻️ Refaktoring" },
  { message = "^chore: bump version", skip = true },
  { message = "^chore", group = "🔧 Ostatní" },
  { message = "^docs",  group = "📝 Dokumentace" },
  { body = ".*security", group = "🔒 Bezpečnost" },
]

protect_breaking_commits = false
filter_commits = true
tag_pattern = "v[0-9].*"
skip_tags = ""
ignore_tags = ""
topo_order = false
sort_commits = "oldest"
